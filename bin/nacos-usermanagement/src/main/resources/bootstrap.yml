spring:
  application:
    name: nacos-userManagement
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        #指定配置中心中配置文件的格式
        file-extension: yml
        #指定文件名
        prefix: ${spring.application.name}
        #DEV命名空间的ID
        namespace: a0d9f516-1135-4de8-898e-db1367d12e3f